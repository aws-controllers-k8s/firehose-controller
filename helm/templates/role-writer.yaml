---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  creationTimestamp: null
  name: {{ include "ack-firehose-controller.app.fullname" . }}-writer
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: {{ include "ack-firehose-controller.app.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/version: {{ .Chart.AppVersion | quote }}
    k8s-app: {{ include "ack-firehose-controller.app.name" . }}
    helm.sh/chart: {{ include "ack-firehose-controller.chart.name-version" . }}
rules:
- apiGroups:
  - firehose.services.k8s.aws
  resources:
  - deliverystreams
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - firehose.services.k8s.aws
  resources:
  - deliverystreams
  verbs:
  - get
  - patch
  - update
